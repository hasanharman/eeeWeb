import { NotTurkishPipe } from './not-turkish.pipe';

describe('NotTurkishPipe', () => {
  it('create an instance', () => {
    const pipe = new NotTurkishPipe();
    expect(pipe).toBeTruthy();
  });
});
